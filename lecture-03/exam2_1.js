const guests = ["Alice", "Bob", "Charlie", "David"];
let index = 0;

while(index < names.length){
    const name = names[index];
    console.log(name)
    // index++;
    // if(index === 'Charlie')
    //     print('Found Charline!');
    
}
// console.log(index);