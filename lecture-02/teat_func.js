function findPerimeteer(length, width){
    const perimeter = (length + width)*2;
    return perimeter;
}

let request = findPerimeteer(10,5);
console.log(`The perimeter is: ${result}`);

// console.log(`The perimeter is: ${findPerimeteer(10,5)}`);